---
status: processed
target: demo-cli
---
# Add a greeting command to demo-cli

New users have no way to sanity-check the install. Something like
`demo-cli greet <name>` would be a nice, obvious smoke test.

Processed: routed to `openspec/changes/pkg-0001-demo/` (see proposal.md).
